# IwaraFlow

Android 原生的 Iwara 竖屏短视频流播放器，目标是提供类似短视频 App 的连续浏览体验。

## 当前功能

### 视频流

- 推荐 / 最新 / 流行 / 人气
- **最新**：直接使用 Iwara 官方 `sort=date`
- **流行**：直接使用 Iwara 官方 `sort=trending`
- **人气**：直接使用 Iwara 官方 `sort=popularity`
- **推荐**：IwaraFlow 本地推荐算法，对多个官方榜单候选重新打分排序
- 上滑下一条、下滑上一条
- 自动加载更多视频
- 播放结束自动进入下一条
- **刷新推荐时排除已看视频**：只在重新生成推荐列表时过滤历史记录，不再在滑动过程中连续自动跳过
- **独立已看记录表**：已看视频 ID 持久化保存，数据库升级时自动迁移旧 history 数据
- **可播放验证**：视频进入推荐 / 最新 / 流行 / 人气 / 搜索列表前，先解析实际 CDN 播放源并进行 Range 探测；确认能返回媒体数据后才显示
- 冷启动优先验证较小候选窗口，缩短首次打开等待时间

### 作者主页 / 关注 / 好友

- 点击视频作者名可进入作者主页
- 作者主页分页展示该作者作品
- 作者作品显示真实 Iwara 视频缩略图
- 不可播放作品保留在作者列表，并在缩略图上显示“仅限好友观看 / 没有权限 / 视频处理中 / 资源失效”等原因
- 点击作者列表中可播放视频后，进入仅包含该作者作品的竖滑播放流
- 作者作品播放流支持首尾循环
- 返回顺序：作者播放流 → 作者作品列表 → 实际进入作者页的页面；从已关注列表进入时先返回已关注列表
- Android 系统返回键 / 全面屏返回手势 / 页面返回按钮使用同一返回逻辑
- 关注状态同步 Iwara 官方数据
- 支持关注 / 取消关注作者
- 主菜单可查看 Iwara 官方“已关注用户”并进入作者主页
- 支持发送好友申请 / 取消好友申请 / 删除好友
- 好友关系状态通过 Iwara 官方接口读取

### 播放器与预加载

- AndroidX Media3 / ExoPlayer
- 默认最高可用画质 / 原画
- 手动切换清晰度
- 单击播放 / 暂停
- 双击点赞 + 爱心动画
- **按住视频约 0.45 秒临时 2× 加速，松手恢复 1×**
- 加速时显示 `2× 加速中`
- Android 画中画（PiP）
- 可设置切到后台自动进入画中画
- 播放器 decoder fallback
- 播放卡死 watchdog：检测到时间轴长时间不前进时自动重建播放源
- 切换视频后旧页面直接停止并释放 ExoPlayer，避免后台继续播放声音
- **全 App 单一播放所有权**：进入作者页 / 作者作品播放器等其他视频页面时会立即停止上一页面播放器，避免两个页面同时出声
- **Media3 磁盘预缓存**：当前视频稳定 READY 后，网络空闲阶段预缓存下一条和下下条的前约 2 MB
- 播放器与预缓存共用同一 CacheDataSource，滑到下一条时可以直接命中已经缓存的数据
- 预缓存使用约 256 MB LRU 上限，自动淘汰旧缓存
- 多 Activity 共用同一个 Media3 SimpleCache，避免重复打开同一缓存目录造成闪退

### Iwara 账号

- Iwara 登录：`/user/login` → `/user/token`
- Token 使用 AndroidX Security 加密存储
- 密码不保存到本地
- Iwara 点赞真实同步：`POST / DELETE /video/{id}/like`
- Iwara 点赞记录：`/favorites/videos`
- Iwara 关注同步
- Iwara 好友关系同步

### 本地收藏

IwaraFlow 的星形收藏是**纯本地收藏**，与 Iwara 官方点赞完全分离：

- `♥` 心形：Iwara 官方点赞，会同步服务端
- `★` 星形：IwaraFlow 本地收藏，仅保存在当前设备 SQLite 数据库

点击点赞不会再同时点亮本地收藏。

### 浏览历史

- SQLite 本地浏览历史
- 保存最近观看的视频
- 保存最后播放位置、视频时长和是否基本看完
- 独立 `seen_videos` 表永久记录推荐系统使用的“已看视频 ID”
- 浏览历史使用独立卡片列表展示
- 本地收藏也有独立列表

### 搜索与下载

- Iwara 视频搜索：`/search?type=videos&query=...`
- 支持搜索标题、标签、作者关键词
- 搜索结果同样经过可播放验证
- 使用 Android DownloadManager 下载
- 下载前可选择视频清晰度

### 应用内更新

- 启动 App 后后台检查 GitHub 最新 Release
- 自动检查间隔约 6 小时，避免频繁请求
- 主菜单提供 **检查更新** 手动入口
- 发现新版本后显示：当前版本、新版本号、更新内容
- 点击“立即更新”直接下载固定签名 `IwaraFlow-signed.apk`
- 下载完成后自动拉起 Android 系统安装界面
- Android 8+ 如未授权“安装未知应用”，会自动进入对应授权页面，返回 App 后继续安装
- 更新 APK 使用与现有安装包相同的固定签名，可直接覆盖升级

## 推荐算法

推荐页不会直接照搬某一个 Iwara 官方榜单。

候选视频来自：

- `trending`，基础权重 3.3
- `popularity`，基础权重 2.8
- `likes`，基础权重 2.1
- `views`，基础权重 1.6

每组抓取前 36 条，同一个视频同时进入多个榜单时会得到额外加分。

随后综合：

- 官方榜单来源权重
- 榜单排名
- 点赞数
- 播放量
- 视频新鲜度
- 本地作者偏好
- 本地标签偏好
- 点赞、收藏、完整观看、下载等本地行为
- 少量稳定扰动，避免推荐结果完全僵化

开启“刷新推荐时排除已看视频”后，`seen_videos` 中的已看视频会在**下一次推荐列表生成时**被直接过滤，不会在当前列表里反复触发自动跳过。

## UI

- 播放主界面保持深色沉浸式风格
- 登录 / 搜索 / 设置弹窗使用浅蓝白色主题
- 登录、保存、搜索等主按钮增加 OEM 主题兼容兜底，确保按钮文字可见
- 搜索和登录输入框改为浅蓝色系
- 浏览历史改为浅蓝背景 + 浅色卡片 + 深色文字
- 作者主页采用浅蓝色卡片列表并显示真实视频缩略图
- 调整卡片圆角、内边距和条目间距
- 画质 / 下载 / 小窗按钮带文字说明，避免只有图标难以理解
- IwaraFlow 专属 Android launcher 图标

## 技术栈

- Kotlin
- ViewPager2
- AndroidX Media3 / ExoPlayer 1.11.0
- Media3 CacheDataSource / SimpleCache
- OkHttp
- Coil
- SQLiteOpenHelper
- AndroidX Security Crypto
- Android DownloadManager
- Android Picture-in-Picture
- GitHub Releases 应用内更新

## 构建

要求：

- JDK 17+
- Android SDK 36
- Gradle 8.13

本地可使用：

```bat
gradlew.bat assembleDebug
```

## GitHub Actions APK / Release

仓库包含自动 Android 构建与发布工作流。

每次推送到 `main` 后会自动：

1. 配置 JDK / Android SDK / Gradle
2. 构建 release APK
3. 使用仓库内固定 keystore 对 APK 签名
4. 使用 `apksigner verify` 验证签名
5. 上传 `IwaraFlow-signed-APK` Actions Artifact
6. 根据 `versionName` 创建或刷新对应 GitHub Release
7. 将 `IwaraFlow-signed.apk` 上传为 Release 资产，供 App 内更新直接下载

> 发布新正式版本时必须同时提高 `versionCode` 与 `versionName`，否则已安装相同版本号的客户端不会弹出更新提示。

## 版本说明

### v0.7.6

- 统一子页面返回与首页播放生命周期，移除返回结果中的定时恢复播放器
- 后台列表加载、卡片绑定和 ViewPager 回调不再获得播放权
- 修复回收卡片复用后遗漏暂停、释放管理的问题，避免旧播放器残留
- 请求提交和关闭使用同一生命周期保护；丢弃已关闭页面的迟到请求及结果，修复返回时续页请求触发线程池拒绝异常的问题
- 作者页、浏览历史、本地收藏、已关注页使用 AndroidX 返回分发；作者视频先退回作者作品列表
- 首页使用标准的 singleTop 启动模式；返回作者页的实际调用者，保留首页版块、视频和进度
- 增加 Android 16 生命周期和真实页面栈回归测试，覆盖页面按钮、系统返回键及侧滑返回
- 菜单新增“诊断信息”：本机保存最近异常、系统退出原因和页面生命周期；不记录账号或视频列表，不自动上传

本地验证命令：`gradle :app:testDebugUnitTest :app:assembleRelease`。
设备测试仅在测试模拟器运行：`gradle :app:connectedDebugAndroidTest`。
设备测试参数 `-Pandroid.testInstrumentationRunnerArguments.backMode=key` 或 `gesture`
分别验证系统按键与手势；测试手势前须启用模拟器的手势导航。
测试使用合成视频、作者和本地会话，不需要真实 Iwara 账号。

### v0.6.3

- 顶部官方排序栏统一为“最新 / 流行 / 人气”
- 最新 = `sort=date`，流行 = `sort=trending`，人气 = `sort=popularity`
- 作者页接入 AndroidX 系统返回分发，兼容返回键和全面屏返回手势
- 返回顺序稳定为作者视频 → 作者列表 → 推荐页
- 新增全 App 单一播放器所有权，进入作者页时强制停止推荐页播放器
- 作者页退到列表时强制停止作者视频播放器
- 冷启动可播放验证候选窗口进一步缩小，减少首次打开等待时间

### v0.6.2

- 作者主页作品列表增加真实 Iwara 视频缩略图
- 图片加载失败才回退到播放图标占位
- 不可播放原因继续覆盖显示在视频缩略图上

### v0.6.1

- 推荐页“排除已看视频”默认开启
- 新增独立 `seen_videos` 持久化表
- 升级时自动迁移旧浏览历史中的已看视频 ID
- 修复作者页与主页同时打开 Media3 缓存目录导致的闪退
- 优化首次可播放验证速度

### v0.6.0

- 新增应用内检查更新
- 启动时后台自动检查新版本
- 主菜单新增“检查更新”
- 更新弹窗展示当前版本 / 新版本 / Release 更新内容
- 支持直接下载固定签名 APK 并拉起系统覆盖安装
- GitHub Actions 自动发布/刷新 GitHub Release APK

### v0.5.0

- 点击作者名进入作者主页
- 作者主页展示作者全部视频并分页加载
- 不可播放作者作品保留并显示具体原因
- 点击作者作品后只在该作者可播放作品中上下滑动
- 新增 Iwara 官方关注 / 取消关注同步
- 新增 Iwara 官方好友申请 / 取消申请 / 删除好友同步
- 主菜单新增已关注用户列表
- 作者播放流返回作者列表，再返回推荐页

### v0.4.0

- “自动跳过已看”改为“刷新推荐时排除已看”，避免连续已看视频造成反复跳转
- 推荐列表刷新时直接过滤历史记录
- 官方排序列表保持 Iwara 官方顺序，不套已看过滤
- 新增 PlayableVideoGate：实际 CDN 源通过轻量 Range 探测后才进入列表
- 不可访问或失效的视频源不会出现在正常视频流中
- 新增 Media3 磁盘预缓存
- 当前视频稳定播放后预缓存下一条和下下条前约 2 MB
- 播放器与预加载共用缓存数据源
- 256 MB LRU 缓存上限

### v0.3.x

- 重构播放器生命周期，减少切页后旧视频继续播放
- 增加 decoder fallback 与自动卡死恢复
- 点赞与本地收藏完全分离
- 优化登录、设置、历史等 UI
- 浏览历史切换为淡蓝色卡片布局
- 搜索 / 登录输入框切换为浅蓝色系
- 新增 IwaraFlow launcher 图标
- 优化画质 / 下载 / 小窗按钮说明
- 新增按住 2× 加速，松手恢复正常速度
- GitHub Actions 改为产出经过签名验证的可安装 APK

### v0.2.0

- 加入 Iwara 登录与 Token 管理
- 加入官方点赞同步
- 加入浏览历史、搜索、下载和画中画
- 加入本地推荐算法
- 加入自动下一条与视频源预解析

## 说明

这是第三方客户端，不隶属于 Iwara。

Iwara API 属于站点内部接口，后续若接口、Cloudflare 策略或视频源签名方式发生变化，相关网络层可能需要继续适配。
