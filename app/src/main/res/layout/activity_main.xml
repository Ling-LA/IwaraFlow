<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical" />

    <!-- 评论面板开着时盖住画面：点画面收起面板，不再传给视频卡片当暂停。
         放在顶栏下面，顶栏照常可点；评论面板在最后，不受影响。 -->
    <View
        android:id="@+id/commentsScrim"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="true"
        android:focusable="true"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="58dp"
        android:layout_gravity="top"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingTop="6dp"
        android:background="#26000000">

        <TextView
            android:id="@+id/tabRecommend"
            android:layout_width="58dp"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="推荐"
            android:textColor="#FFFFFFFF"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- Keep existing IDs/mappings, but present them in official Iwara order:
             date = 最新, trending = 流行, popularity = 人气. -->
        <TextView
            android:id="@+id/tabLatest"
            android:layout_width="58dp"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="最新"
            android:textColor="#99FFFFFF"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/tabTrending"
            android:layout_width="58dp"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="流行"
            android:textColor="#99FFFFFF"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/tabPopular"
            android:layout_width="58dp"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="人气"
            android:textColor="#99FFFFFF"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/btnSearch"
            android:layout_width="50dp"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="⌕"
            android:textColor="#FFFFFFFF"
            android:textSize="27sp" />

        <TextView
            android:id="@+id/btnMenu"
            android:layout_width="50dp"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="⋮"
            android:textColor="#FFFFFFFF"
            android:textSize="29sp" />
    </LinearLayout>

    <ProgressBar
        android:id="@+id/loading"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center" />

    <TextView
        android:id="@+id/error"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:padding="24dp"
        android:textColor="#FFFFFFFF"
        android:textSize="15sp"
        android:visibility="gone" />

    <!-- 评论面板放最后，盖在视频流和顶栏之上；高度由代码按视频画面算。 -->
    <include layout="@layout/view_comments_panel" />
</FrameLayout>
